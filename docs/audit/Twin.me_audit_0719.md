# Twin.me 통합 감사 — 2026.07.19

> **분석 기준:** `Twin_me_UX_Persona_Review_v4_PDF_vs_App.md` + `TwinMe_신_창업기획서.pdf`(v2.6) + `Twin_me_구현_현황_보고서_v3.pdf`(MASTER v2.7) + 코드베이스(`Twin_me_-main.zip`) 전수 대조
> **핵심 관점:** 문서(설명서/사업계획서/구현현황보고서)가 밝힌 내용 ↔ 실제 코드 구현 상태의 gap 종합

---

## 종합 표

| 우선 | 분류 | 항목 | 현재 상태 (코드 근거) | 필요한 조치 |
|---|---|---|---|---|
| **P0** | 표시 오류 | **RDCS v2.1(DNA 일치율) 카드가 기본 화면에 안 보임** | `useFeatureDnaV21()` 기본값 OFF → `genesis.tsx`/`index.tsx`가 `dnaV21` 조건부 렌더. 구현현황보고서가 자랑하는 DnaCompatibilityCard·GenesisV21Screen이 플래그 꺼진 채로는 전혀 노출 안 됨 | 투자자 데모 전 플래그 ON 확정 또는 온보딩에서 자동 활성화 |
| **P0** | 버그 | **콜드스타트 온보딩 오탐 (스플래시 레이스컨디션)** | `splash.tsx`가 `_hasHydrated`를 기다리지 않고 애니메이션 종료 시점에 `isOnboardingComplete`를 즉시 읽음. AsyncStorage 복원이 늦으면 재방문 유저가 웰컴 화면으로 튕김 | `onRehydrateStorage` 완료 대기 로직 추가 |
| **P0** | 미구현 | **Helix 아카이브 — 실 카카오데이터 연동 0%** | `hasKakaoData=true`여도 `HELIX_CARDS` 하드코딩 6개가 그대로 노출(뱃지만 제거). `MOCK_PLACES_COUNT=4`도 항상 고정 | kakaoParser 결과→HelixCard 변환 파이프라인 신규 개발 |
| **P0** | 미구현 | **Magic Mirror 발신 엔진 자체가 없음** | `MagicMirrorModal.tsx`(134줄)는 동의 UI뿐. §4.6의 "100개당 1회 모니터링→전환율 12%→월 4회 발신" 로직/서비스 코드 0건 | 2단계 필터링 발신 서비스 신규 개발 |
| ~~P1~~ | **범위 제외** | ~~카카오맵 SDK 실연동 안 됨~~ | `history.tsx` 888행 `// TODO: 카카오맵 SDK 연동 후 실제 지도 렌더링` | **AI 데이트코스 추천 기능 전체와 함께 별도 세션에서 단독 진행** — 이번 스코프 제외 |
| ~~P1~~ | **범위 제외** | ~~AI 데이트코스 지도 핀+연결선 시각화~~ | `date-recommend-result.tsx` 5행 "v1에서 스킵" | **별도 세션 진행** — 이번 스코프 제외 |
| **P1** | 미구현 | **소셜 로그인** | 코드 전체 카카오/OAuth 관련 0건 (사업계획서도 향후 과제로 자인) | 카카오 소셜 로그인 신규 구현 |
| **P1** | 미검증 | **IAP 실기기 결제 미검증** | `iapService.ts` 상단 주석 "1회 실기기 검증 필요" — 현재는 샌드박스 폴백(가짜 결제 시뮬레이션)만 확인됨 | TestFlight/실기기 결제 흐름 검증 |
| ~~P1~~ | **범위 제외** | ~~Sigma 테마 온보딩 유도 화면 없음~~ | `genesis.tsx`/`invite-hook.tsx`에 "Sigma/아우라 테마" 언급 0건 | **오라 시스템(테마) 전체와 함께 별도 세션에서 단독 진행** — 이번 스코프 제외 |
| **P1** | 미구현 | **주간 리포트 서버 자동발송 스케줄러 없음** | `weeklyReportService.ts`·Edge Functions 전체에 cron/schedule 코드 0건 (스펙: 매주 일요일 22시) | Supabase cron/pg_cron 또는 스케줄 Edge Function 신설 |
| **P1** | 미구현 | **포그라운드 알림 인앱 분기 없음** | `useNotifications.ts` 수신 리스너 주석 "배너는 OS 설정을 따르고 별도 처리는 하지 않는다" | 크라이시스/매직미러 알림용 인앱 커스텀 배너 로직 추가 |
| **P2** | 미착수 | **Instagram DM 연동 (FUN-ONB-004)** | 코드 전체 `Instagram` 키워드 0건, 스펙에도 "개발 대기 상태" 명시 | 우선순위 낮음, 별도 스프린트 |
| **P2** | 잔존 코드 | **console.log 프로덕션 잔존** | `AuraDuskGradient.tsx` 92행, `index.tsx` 153행 | 배포 전 제거 |
| **P2** | UI 결함 | **라이트 테마 하드코딩 색상** | `settings.tsx` 등에 `#FFFFFF` 다수 하드코딩 — 다크 전용 값이 라이트 모드 대비 문제 유발 여지 | `getText(isDark)` 토큰으로 교체 |
| **P2** | 서술-앱 불일치 | **웰컴 화면 카피 온도 차이** | 설명서 "친애하는 당신께" 톤 vs 앱 실제 "나중에 둘러볼게요" 등 구어체 | 카피만 톤 통일 (기능 영향 없음) |
| 참고 | 문서 간 불일치 | **DateMapView 잔여과제 서술이 낡음** | 마스터 스펙은 "제거됨" 확정, 구현현황보고서는 여전히 "dead code→연결" 서술 | 코드 조치 불필요, 문서만 최신화. 실질 작업은 위 "카카오맵 SDK 연동" 항목으로 대체 이해 |
| 참고 | 해결됨(조치 불필요) | 연인방 잠금 화면 초대 UX | `chat.tsx` `loverLockScreen`에 초대코드+공유 버튼 인라인 CTA 이미 구현 | 없음 |
| 참고 | 해결됨(조치 불필요) | 로그인/가입 빈 입력 비활성화 | `login.tsx`/`signup.tsx` `disabled` 조건 이미 존재 | 없음 |
| 참고 | 해결됨(조치 불필요) | Wrapped 페이월 게이팅 | `WrappedModal.tsx`가 `usePremiumGate` 기준 lockOverlay 정상 작동 | 없음 |
| 참고 | 해결됨(조치 불필요) | LLM API 키 클라이언트 노출 우려 | 전 서비스가 `callLLM()`→Edge Function 프록시 경유 확인 | 없음 |

---

## 요약

- **P0 4건** (투자자 데모 리스크 직결 3건 포함)
- **P1 5건** (기존 7건 중 2건은 아래 스코프 제외로 이동)
- **P2 3건**
- **참고 5건** (4건 이미 해결, 1건 문서 정정만 필요)
- **범위 제외 2건** — AI 데이트코스 추천 관련 지도 SDK/시각화, Sigma 테마(오라 시스템) 온보딩. 각각 별도 세션에서 단독 진행 예정이므로 이번 스코프의 작업 대상에서 제외.

7/22 투자자 데모 전에는 P0 항목만이라도 처리하는 것이 가장 임팩트가 큼. 특히 **RDCS v2.1 플래그 미노출**은 구현현황보고서가 핵심 성과로 내세우는 기능이 기본 상태에서 투자자에게 전혀 보이지 않는다는 뜻이므로 최우선 확인 필요. (참고: RDCS v2.1은 오라/6-Sigma 테마와는 별개 시스템이므로 이번 스코프에 포함됨 — 제외 대상 아님.)

---

*작성 기준일: 2026-07-19*
*분석 소스: UX 페르소나 리뷰(v4) · 서비스 창업 기획서(v2.6) · 구현 현황 보고서(v3, MASTER v2.7) · 코드베이스 전수 대조*
